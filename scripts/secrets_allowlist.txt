# Documented false positives for scripts/secrets_scan.sh (F0.4).
# Each line is a literal substring; matching lines are excluded from findings.
# sk-ant-test-leak-canary-12345: deliberate fake canary in
#   backend/tests/test_phase17_anthropic_provider.py verifying keys never leak
#   into error messages. Not a live credential.
sk-ant-test-leak-canary-12345
