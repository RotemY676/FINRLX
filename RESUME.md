# PROGRAM LEAP — RESUME marker (§2.2)
Backend track DONE through: F0, F1, F2-core, S2, S3, S4, S6-backend, S8.
Next tasks, in order:
1. S9 backend: "why this matters" annotation job — strict contract (Appendix E.2),
   adversarial validator (advice-verbs, unbound sources, >2 sentences), 20-item
   canary, INSIGHTS_ANNOTATIONS flag default OFF; feeds dossier news card.
2. F2 leftover: session-filtered ingest ranges (low priority).
3. C1 backend prep: state-drift check script wired to ci_gate.
4. Frontend track (Claude Code + node toolchain + rotated token E1):
   S1 wireframes -> S5 One Screen -> S6 compare UI -> S7 /pro migration ->
   F1.6 staleness badges -> F3 sweeps -> C1 close.
Delete when S9 backend closes.
