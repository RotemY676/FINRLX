# FinRL-X CPU-only research dependencies.
# LOCAL RESEARCH ONLY — not used by Railway production backend.
# Do NOT merge into backend/requirements.txt.

numpy>=1.26,<2.0
pandas>=2.1,<3.0
gymnasium>=0.29,<1.0
stable-baselines3>=2.3,<3.0
torch>=2.2,<3.0 --index-url https://download.pytorch.org/whl/cpu
